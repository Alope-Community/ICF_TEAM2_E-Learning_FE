import { useEffect } from "react";
import { useParams } from "react-router-dom";

const Course = () => {
  const id = useParams();
  console.log(id);

  useEffect(() => {
    
  });

  return <></>;
};

export default Course;
